---
- name: Configure all instances
  hosts: all
  become: yes
  tasks:
    - name: Install required packages
      apt:
        name: "{{ item }}"
        state: present
        update_cache: yes
      loop:
        - apt-transport-https
        - ca-certificates
        - curl
        - gnupg
        - lsb-release

    - name: Add Docker GPG key
      apt_key:
        url: https://download.docker.com/linux/ubuntu/gpg
        state: present

    - name: Add Docker APT repository
      apt_repository:
        repo: "deb [arch=amd64] https://download.docker.com/linux/ubuntu focal stable"
        state: present

    - name: Install Docker and dependencies
      apt:
        name: [docker-ce, docker-ce-cli, containerd.io]
        state: latest
        update_cache: yes

    - name: Install Docker Compose
      get_url:
        url: https://github.com/docker/compose/releases/download/v2.24.5/docker-compose-linux-x86_64
        dest: /usr/local/bin/docker-compose
        mode: '0755'

    - name: Ensure Docker is started and enabled
      systemd:
        name: docker
        state: started
        enabled: yes

    - name: Add user to docker group
      user:
        name: "{{ ansible_user }}"
        groups: docker
        append: yes

        
- name: Deploy frontend stack
  hosts: frontend
  become: yes
  tasks:
    - name: Copy docker-compose.yml
      copy:
        src: ./compose/frontend/docker-compose.yml
        dest: /home/ubuntu/docker-compose.yml
        mode: '0644'

    - name: Launch frontend containers
      shell: docker-compose up -d
      args:
        chdir: /home/ubuntu

- name: Deploy backend stack
  hosts: backend
  become: yes
  tasks:
    - name: Copy docker-compose.yml
      copy:
        src: ./compose/backend/docker-compose.yml
        dest: /home/ubuntu/docker-compose.yml
        mode: '0644'

    - name: Launch backend containers
      shell: docker-compose up -d
      args:
        chdir: /home/ubuntu

- name: Deploy database stack
  hosts: database
  become: yes
  tasks:
    - name: Copy docker-compose.yml
      copy:
        src: ./compose/database/docker-compose.yml
        dest: /home/ubuntu/docker-compose.yml
        mode: '0644'

    - name: Launch database container
      shell: docker-compose up -d
      args:
        chdir: /home/ubuntu
